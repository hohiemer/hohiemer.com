{% extends "src/_includes/layouts/work-base.njk" %}

{% block gallery %}
  <div class="gallery bios">
    {% for card in cards %}
      <section class="card">
      <h2>{{ card.title }}</h2>
      <p>{{ card.content }}</p>
      </section>
    {% endfor %}
  </div>
{% endblock %}